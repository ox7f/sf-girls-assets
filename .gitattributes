# Track large binary files with Git LFS
*.png filter=lfs diff=lfs merge=lfs -text
*.ogg filter=lfs diff=lfs merge=lfs -text
*.moc3 filter=lfs diff=lfs merge=lfs -text

# Don't track text files with LFS
*.md text
*.atlas text
*.json text
*.txt text
